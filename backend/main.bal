// Main entry point is not strictly necessary when services are attached to listeners directly in modules.
// However, Ballerina requires at least one .bal file in the root package directory.
import trainlk/backend.api as _; 
